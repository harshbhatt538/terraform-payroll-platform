name: Deploy — Bureaus Portal

on:
  push:
    branches:
      - main
    paths:
      - 'docker/app/**'
      - '.github/workflows/deploy-bureaus.yml'

env:
  SERVICE_NAME: bureaus
  AWS_REGION: ap-south-1

permissions:
  contents: read
  packages: write

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t ${{ env.SERVICE_NAME }}:test ./docker/app

      - name: Run container tests
        run: |
          docker run -d \
            --name test-${{ env.SERVICE_NAME }} \
            -p 8080:8080 \
            -e SERVICE_NAME=${{ env.SERVICE_NAME }} \
            -e APP_ENV=test \
            ${{ env.SERVICE_NAME }}:test

          sleep 3
          curl -f http://localhost:8080/health
          curl -f http://localhost:8080/
          docker stop test-${{ env.SERVICE_NAME }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push image to registry
        run: |
          IMAGE_TAG=${{ github.sha }}
          docker tag ${{ env.SERVICE_NAME }}:test \
            ghcr.io/${{ github.repository }}/${{ env.SERVICE_NAME }}:$IMAGE_TAG
          docker push \
            ghcr.io/${{ github.repository }}/${{ env.SERVICE_NAME }}:$IMAGE_TAG

  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build-and-test
    environment: dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate deployment configuration
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          INSTANCE_ID: ${{ vars.BUREAUS_INSTANCE_ID }}
        run: |
          if [ -z "$AWS_ACCESS_KEY_ID" ]; then echo "::error::Missing repository secret AWS_ACCESS_KEY_ID"; exit 1; fi
          if [ -z "$AWS_SECRET_ACCESS_KEY" ]; then echo "::error::Missing repository secret AWS_SECRET_ACCESS_KEY"; exit 1; fi
          if [ -z "$INSTANCE_ID" ]; then echo "::error::Missing repository variable BUREAUS_INSTANCE_ID"; exit 1; fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy via SSM
        run: |
          aws ssm send-command \
            --instance-ids "${{ vars.BUREAUS_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --parameters '{"commands":["GITHUB_REPOSITORY=${{ github.repository }} bash /home/ec2-user/deploy.sh ${{ env.SERVICE_NAME }} dev ${{ github.sha }}"]}' \
            --comment "Deploy ${{ env.SERVICE_NAME }} ${{ github.sha }}" \
            --region ${{ env.AWS_REGION }}
